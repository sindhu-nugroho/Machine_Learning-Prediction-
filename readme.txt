buat venv = python3.10 -m venv venv
aktifkan venv = venv\Scripts\activate

pandas numpy matplotlib statsmodels openpyxl = sarima

pip install pandas numpy scikit-learn scipy matplotlib = hierarchichal clustering

pip install pandas xgboost matplotlib = xgboost
